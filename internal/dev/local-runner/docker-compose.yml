# OpsPulse local demo — Dev deployment skeleton
# Phase 2 will wire SERVICE_NAME and image from Issue Spec.

services:
  order-service:
    image: "${JDK_BASE_IMAGE:-eclipse-temurin:8-jre}"
    container_name: opspulse-order-service-demo
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    # Phase 2: replace with built service image
    # build:
    #   context: ../examples/fixtures
    #   dockerfile: deploy/order-service/Dockerfile
    #   args:
    #     JDK_BASE_IMAGE: ${JDK_BASE_IMAGE:-eclipse-temurin:8-jre}
    profiles:
      - demo
