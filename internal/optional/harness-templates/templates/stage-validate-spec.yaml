# Stage template: Validate Issue Spec
# Used by pipeline-pr-validation stage 1

stage:
  name: Validate Issue Spec
  identifier: validate_spec
  type: CI
  description: |
    Run scripts/validate-issue-spec.py against the Issue markdown.
    Gate: opspulse_version, service.name, runtime.jdk_base_image, build, acceptance.
